import { LinkedBankAndCardsList } from "./components";
import { SectionContainer, SectionTitle } from "./styled-components";

export function BankAccountAndCards(){
    return (
        <SectionContainer>
            <SectionTitle>Bank Account And Cards</SectionTitle>
            <LinkedBankAndCardsList/>
        </SectionContainer>
    );
}